#include <stdio.h>

int main()
{

    int hoursWorked, overtime, depedents;
    float salary;

    printf("=================Insert the values for Salary, Hours Worked, Overtime and depedents.\n");
    printf("Salary: ");
    scanf("%f", &salary);
    printf("Hours Worked: ");
    scanf("%f", &hoursWorked);
    printf("Overtime: ");
    scanf("%f", &overtime);
    printf("Depedents: ");
    scanf("%f", &depedents);

    return 0;
}